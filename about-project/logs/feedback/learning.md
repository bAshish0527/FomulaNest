# Learning Log

- Formulas are stored in the notes renderer in the client script and are selected by class and subject.
- Blank screens usually happened when the server was not running or the LAN IP changed.
- PWA caching can show stale pages if the cache is not refreshed.
- WhatsApp Web does not open inside embedded browsers; use mobile or system browser.
- AI chat uses OpenRouter and needs an API key set in the server environment.
- Practice now points to a dedicated page that launches class quizzes directly.
